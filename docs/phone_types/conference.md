# Conference

A phone conference can be used to gather several people in one room.

## Prerequesites

Add `conference` to the list of enabled flavors in your config.

## Usage

Create a new extension with the type `Conference`. People can
now call the extension to join into the conference.