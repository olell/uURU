# Dummy

An extension that isn't really an extension.

## Prerequesites

Add `dummy` to the list of enabled flavors in your config.

## Usage

You can create dummy extension and give them a name. They can
be used to display dialplan entries you manually fumbled into
the asterisk database.